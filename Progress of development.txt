02.06.2025
1) Я добавил в MockAPI массив юзеров. Теперь посты содержатся в связанном массиве. Обращение через /users-collection/1/posts, где (1) - это id пользователя.
-> мне нужно всего лишь изменить запрос к серверу GET (в дальнейшем и POST), чтобы отображать посты конкретного пользователя.

2) В GET не передавался id всё это время, т.к. рендерил только один профиль. Чтобы это исправить, я стал принимать params, где в выходных данных был id. Я проверил, что он меняется в зависимости от профиля и стал формировать запрос на его основе. (Важно: всегда нужно передавать request помимо params, иначе params будет undefined)
- 2.1 - разделил запросы для получения постов и получения данных профиля. Теперь они загружаются отдельно. Создал стейт для хранения данных юзера и передал его данные в блок для рендера (тут возникала ошибка типа (что может быть undefined), решилось быстро - просто сделал условный рендер, что есть userData)
- 2.2 - Пришлось также изменить сами пути, т.к. они теперь исходят из юзера. Теперь любой запрос к серверу протекает через получение id (чтобы работать с конкретным пользователем). Возникла странная ошибка с параметрами: `const { id } = params;` - отображалась в логе ошибка, что нужно дождаться получения параметров, но всё рендерилось. Как понимаю, в самом начале просто данных не было, а попытки рендера были, затем стейт обновлялся и всё отображалось. Решилось добавлением `await` переж `params`.

3) Сейчас в Апишке всего 10 юзеров, поэтому при запросе по id=11 отобразится встроенная NotFound-страница. Поэтому нужно сделать в папке специальную для этого блока. Добавить условие, которое будет редиректить на неё, если id>data.length(это мы через доп. запрос вычислим общее количество объектов, чтобы работать с ними)


Сделать:
- страницу со всеми пользователями
- фильтрацию постов
- пагинацию постов
- авторизация
- лайки

Поправить:
- аватар юезра обернуть в блок, чтобы не растягивался на всё доступное пространство (или посмотреть на адаптивность в мобилке, если окей, то ладно)
- Все классы обернуть в CVA для удобства



03.06.2025
1) Взялся за реализацию страницы со всеми юзерами, чтобы переходить в профили не через адресную строку. Для этого я создал отдельный роут `/users` со страницей, где буду запрашивать всех пользователей.
2) Обнаружил ошибку, что у новых пользователей нет постов, поэтому рендер постов завершается с ошибкой. Добавил условный рендеринг.
3) Немного улучшил главную страницу и добавил там кнопку для перехода к списку всех юзеров.
4) Реализую функцию удаления постов. Для этого в UserPosts создал кнопку, в которую буду передвавать хэндлер. Сам хэндлер создаётся в родителе и ссылкается на метод DELETE из Api.